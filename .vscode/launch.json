{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with CodeLLDB",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/homework/linear_equations/main", // Path to your executable
            "args": [], // Command-line arguments
            "cwd": "${workspaceFolder}", // Working directory
            "stopOnEntry": true,  // Stop at main()
            "terminal": "integrated", // Use the integrated terminal
            "sourceMap": {
                "/build": "${workspaceFolder}" // Map build paths to source paths (if needed)
            }
        }
    ]
}